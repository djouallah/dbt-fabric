# iceberg: dbt-duckdb writing through the OneLake Iceberg REST catalog.
#
# LATEST PRE-RELEASE, UNPINNED. Compaction (.github/scripts/compact_iceberg.py) needs
# iceberg_rewrite_data_files(), which exists only on the 1.6.0 dev line — so this needs a
# pre-release, but it floats rather than sitting on one hand-bumped build. The DuckDB
# extension binaries (azure/iceberg/avro) are keyed to the duckdb build, so whatever this
# resolves to pins the iceberg extension with it.
#
# The `--pre` is what makes pip consider dev builds at all; without it pip resolves the
# newest STABLE duckdb and compaction silently loses its rewrite function (the script
# already degrades gracefully via has_rewrite_function()).
--pre
duckdb
duckrun
