# iceberg: dbt OSS 2, writing through the OneLake Iceberg REST catalog.
#
# THIS IS THE WHOLE LIST. dbt v2 is a self-contained engine -- it bundles its own DuckDB and
# needs no adapter package, so there is no dbt-duckdb here and no `duckdb` pip either. The
# project it builds is dbt2/.
#
# WHO INSTALLS THIS: the Fabric notebook (.github/scripts/remote_dbt.py reads this file and
# pip-installs it there) and the free gating job in ci.yml. The RUNNER installs
# requirements/iceberg_runner.txt instead -- see that file for why the two cannot be one.
#
# PINNED, unlike the duckdb legs. v2 is four days old at time of writing and its
# catalogs.yml v2 spec still logs "experimental ... liable to change" (dbt1701), so a float
# here would let the spec move under a leg that has no local reproduction.
dbt-oss==2.0.4
