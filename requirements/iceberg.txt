# iceberg: dbt-duckdb writing through the OneLake Iceberg REST catalog. Project: dbt1/.
#
# It was dbt-oss==2.0.4 for one day (2026-09-17 to 2026-09-18). That engine could not build a
# single model against this catalog -- GetTableInformation returned BadRequest_400 "Malformed
# request" on the first one -- so the leg is back on dbt-duckdb, and there is no _runner.txt
# twin any more: dbt-duckdb and duckrun share an environment happily, it was dbt-oss that
# could not (duckrun pins dbt-core<2 and lays a 1.x `dbt` script over v2's).
#
# LATEST PRE-RELEASE duckdb, UNPINNED, and `--pre` IS LOAD-BEARING TWICE OVER:
#   * the profile attaches the catalog with VENDED CREDENTIALS and no azure secret. The
#     vended adls.sas-token.onelake.dfs.fabric.microsoft.com is only usable since
#     duckdb/duckdb-iceberg#1331 (merged 2026-08-19), which is not in a stable duckdb yet.
#     dbt OSS 2 bundling duckdb 1.5.3 is exactly why IT had to bring its own secret.
#   * .github/scripts/compact_iceberg.py needs iceberg_rewrite_data_files(), same line.
# Without `--pre` pip resolves the newest STABLE duckdb and the vend fails at the first write.
# dbt-duckdb declares `duckdb>=1.0.0` with no upper cap and duckrun `duckdb>=1.5.4`, so
# nothing here holds the resolution back to the stable line.
#
# The DuckDB extension binaries (azure/iceberg/avro) are keyed to the duckdb build, so
# whatever this resolves to pins the iceberg extension with it.
#
# WHO INSTALLS THIS: the runner (pipeline.yml), the Fabric notebook (remote_dbt.py reads this
# file and pip-installs it there) and the free gating job in ci.yml -- one list for all three.
# duckrun is in it for provisioning and for launching the notebook, not for dbt.
--pre
duckdb
dbt-duckdb
duckrun
