# The free offline checks: `python -m pytest tests_py/ -q`.
#
# NO dbt HERE ANY MORE. tests_py used to pin macros/iceberg_adapter_overrides.sql against
# the INSTALLED dbt-duckdb, which meant the unit job had to install an adapter. That file is
# gone -- once the iceberg engine moved to dbt2/ its only live branch was dead, and dbt v2
# ships its own Iceberg-aware DuckDB macros -- so the unit tests are now pure file reading
# and sqlglot parsing, and install neither dbt nor an adapter.
#
# pyyaml is explicit rather than inherited: it used to arrive transitively via dbt-core.
pytest
pyyaml
sqlglot
