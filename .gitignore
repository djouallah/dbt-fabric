# dbt
target/
logs/
dbt_packages/
.user.yml

# Python
__pycache__/
*.py[cod]
.pytest_cache/
*.db

# Local landing zone for `python download_aemo.py` with FILES_PATH=./landing
landing/
warehouse/
.duckrun_spill/

# Windows / tooling artefacts
nul
%SystemDrive%/
.claude/
blog.md
PLAN.md

# Parity fingerprints are written by CI; a laptop run would poison the comparison.
history/parity/*.json
